import React from "react";
import {Add,Folder,FolderOpen,Description,Edit,Delete} from '@material-ui/icons';

const News = ({name}) => {
  return (
   <div>
     News
   </div>
  );
};

export default News;
